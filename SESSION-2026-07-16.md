# SESSION 2026-07-16 — hellosansit.com

> Handoff odtworzony po zerwaniu połączenia (~15:00). Pliki na dysku kompletne
> (ostatni zapis 14:55), stracił się tylko kontekst rozmowy — decyzje otwarte
> poniżej wymagają potwierdzenia Kuby.

## Stan repo
- branch `main`, commity:
  - `ed4c9ae` — struktura projektu wg PRD v2 (`_DOCS`, `video`, `emotion`, `character`, `logo`, `audio`)
  - `c24b582` — wideo hero (intro/loop pearl TEST + 3 warianty odcieni) + font Nunito Variable
- `.gitignore`: `_tools/` i `_work/` NIE są wersjonowane (narzędzia lokalne + klatki pośrednie)

## Co powstało dziś

### Pipeline wymiany tła wideo (Sansit → jednolita perła)
- `_tools/matting_pipeline.py` — pełny pipeline: klatki (ffmpeg) → grade (curves + saturacja 1.18)
  → maski (rembg, model `isnet-general-use`) → composite na perłę `#F5F1EA` + **stabilny cień**
  → mp4 (x264 crf 20, faststart). Cache klatek/masek w `_work/<clip>/`.
- **Cień kotwiczony do STREFY STÓP** (dolne 40px maski), nie do bboxa całej postaci —
  bbox powodował „jeżdżenie" cienia gdy postać macha rękami. Wygładzenie: mediana krocząca okno 13.
- `_tools/bake_variant.py` — wypala wariant kolorystyczny tła z cache'owanych masek/graded
  (`python bake_variant.py loop F8F5EF ../video/loop_F8F5EF.mp4`). Dodatkowo wymusza bt709
  full→tv range, żeby przeglądarki nie przekłamywały koloru.
- Wynik: `video/intro_pearl_TEST.mp4`, `video/loop_pearl_TEST.mp4` + 3 warianty odcieni loopa:
  `F8F5EF` (jaśniejsza kość słoniowa), `F6EFE0` (kremowa), `F2EFEA` (chłodna porcelana).
- Źródła: `video/intro.mp4`, `video/loop.mp4` (oryginalne, sprzed pipeline'u).

### Demo sceny — `_tools/scena-final.html` (ostatni zapis 14:55)
Kompletna scena hero + emocje:
- header sticky (logo 3D, EN|PL, mini CTA)
- hero: sklejka **intro → loop** (loop startuje 0.45 s przed końcem intro, crossfade opacity)
- **auto-sampling tła strony z rogu wideo** (canvas 32×32 z prawego górnego rogu) —
  gwarantuje zero szarej ramki niezależnie od interpretacji kolorów przeglądarki
- glow za kursorem (radial-gradient zieleń→fiolet, rAF-throttled)
- galeria 9 emocji (wybór Kuby 2026-07-16): joy2, curiosity1, sad5, anger2, fear3-ok,
  disgust2, surprised2, calm1, frustracja3
- font: Nunito Variable (`fonts/`), waga 200–1000
- **panel roboczy** (nie element strony): przełącznik odcienia perły (4 warianty)
  + kolor CTA (złoto `#f2b334` / zieleń `#7CBF6B` / magenta `#E05FD0` / fiolet `#6B2FD9`)

### Pozostałe narzędzia w `_tools/`
- `emotion-picker.html` — wybór wariantów emocji (użyty, wybór wyżej)
- `hero-blend-playground.html`, `intro-loop-test.html` — przymiarki blendowania/sklejki
- `blend-test/` — klatki porównawcze, `realesrgan/` — upscaler lokalny

## DECYZJE OTWARTE (do potwierdzenia — kontekst rozmowy przepadł)
- [ ] **Odcień perły**: bazowy `#F4EFE8` vs 3 warianty z panelu
- [ ] **Kolor CTA**: złoto / zieleń / magenta / fiolet
- [ ] Po wyborze odcienia: wypalić FINALNE intro+loop w tym kolorze (`bake_variant.py`,
      intro też — TESTy mają intro tylko w bazowym)

## Następne kroki
1. Kuba ogląda `scena-final.html`, klepie odcień perły + kolor CTA
2. Wypalenie finalnych wideo w wybranym kolorze, podmiana TESTów
3. Przeniesienie sceny z `_tools/` do właściwego `index.html` (produkcyjnie: bez panelu roboczego)
4. Reszta strony wg `_DOCS/SANSIT_WEBSITE_PRD_v2.md` (+ `SANSIT_ABOUT_brief.md`)
5. PRD dokłada Kuba dziś (2026-07-16) — sprawdzić czy v2 w `_DOCS` jest już aktualne
