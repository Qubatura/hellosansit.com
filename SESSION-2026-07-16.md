# SESSION 2026-07-16 — hellosansit.com

> Handoff odtworzony po zerwaniu połączenia (~15:00). Pliki na dysku kompletne
> (ostatni zapis 14:55), stracił się tylko kontekst rozmowy — decyzje otwarte
> poniżej wymagają potwierdzenia Kuby.

## Stan repo
- branch `main`, commity:
  - `ed4c9ae` — struktura projektu wg PRD v2 (`_DOCS`, `video`, `emotion`, `character`, `logo`, `audio`)
  - `c24b582` — wideo hero (intro/loop pearl TEST + 3 warianty odcieni) + font Nunito Variable
- `.gitignore`: `_tools/` i `_work/` NIE są wersjonowane (narzędzia lokalne + klatki pośrednie)

## Co powstało dziś

### Pipeline wymiany tła wideo (Sansit → jednolita perła)
- `_tools/matting_pipeline.py` — pełny pipeline: klatki (ffmpeg) → grade (curves + saturacja 1.18)
  → maski (rembg, model `isnet-general-use`) → composite na perłę `#F5F1EA` + **stabilny cień**
  → mp4 (x264 crf 20, faststart). Cache klatek/masek w `_work/<clip>/`.
- **Cień kotwiczony do STREFY STÓP** (dolne 40px maski), nie do bboxa całej postaci —
  bbox powodował „jeżdżenie" cienia gdy postać macha rękami. Wygładzenie: mediana krocząca okno 13.
- `_tools/bake_variant.py` — wypala wariant kolorystyczny tła z cache'owanych masek/graded
  (`python bake_variant.py loop F8F5EF ../video/loop_F8F5EF.mp4`). Dodatkowo wymusza bt709
  full→tv range, żeby przeglądarki nie przekłamywały koloru.
- Wynik: `video/intro_pearl_TEST.mp4`, `video/loop_pearl_TEST.mp4` + 3 warianty odcieni loopa:
  `F8F5EF` (jaśniejsza kość słoniowa), `F6EFE0` (kremowa), `F2EFEA` (chłodna porcelana).
- Źródła: `video/intro.mp4`, `video/loop.mp4` (oryginalne, sprzed pipeline'u).

### Demo sceny — `_tools/scena-final.html` (ostatni zapis 14:55)
Kompletna scena hero + emocje:
- header sticky (logo 3D, EN|PL, mini CTA)
- hero: sklejka **intro → loop** (loop startuje 0.45 s przed końcem intro, crossfade opacity)
- **auto-sampling tła strony z rogu wideo** (canvas 32×32 z prawego górnego rogu) —
  gwarantuje zero szarej ramki niezależnie od interpretacji kolorów przeglądarki
- glow za kursorem (radial-gradient zieleń→fiolet, rAF-throttled)
- galeria 9 emocji (wybór Kuby 2026-07-16): joy2, curiosity1, sad5, anger2, fear3-ok,
  disgust2, surprised2, calm1, frustracja3
- font: Nunito Variable (`fonts/`), waga 200–1000
- **panel roboczy** (nie element strony): przełącznik odcienia perły (4 warianty)
  + kolor CTA (złoto `#f2b334` / zieleń `#7CBF6B` / magenta `#E05FD0` / fiolet `#6B2FD9`)

### Pozostałe narzędzia w `_tools/`
- `emotion-picker.html` — wybór wariantów emocji (użyty, wybór wyżej)
- `hero-blend-playground.html`, `intro-loop-test.html` — przymiarki blendowania/sklejki
- `blend-test/` — klatki porównawcze, `realesrgan/` — upscaler lokalny

## DECYZJE (potwierdzone przez Kubę po zerwaniu sesji, 07-16)
- [x] **Odcień perły: `#F2EFEA`** — chłodna porcelana (najzimniejszy wariant z panelu)
- [x] **Kolor CTA: fiolet `#6B2FD9`** (ink biały `#fff`)
- ✅ Sugestie i obawy Kuby ODTWORZONE z głowy (~16:00) — patrz sekcja „Feedback Kuby" niżej.

## Feedback Kuby do sceny (odtworzony po zerwaniu, 07-16)
1. **BUG intro→loop**: intro się kończy i loop się nie pojawia → NAPRAWIONE
   (loop miał `opacity:0` w CSS i nic nie podnosiło do 1; do tego panel roboczy
   odwoływał się do `lv` przed deklaracją → ReferenceError; dodany też fallback
   na `ended`, gdyby `timeupdate` nie zdążył)
2. **Header ucina glow kreską u góry** → NAPRAWIONE (header przezroczysty na górze
   strony, tło+blur dopiero po scrollu; glow wystaje 80px ponad hero)
3. **Teksty** — do omówienia osobno (na razie placeholdery)
4. **Logo za małe** — ma mieć wysokość ~guzika „Get the book" w headerze → PODBITE
   do 52px (PNG ma duże przezroczyste marginesy, napis to ~połowa wysokości pliku;
   docelowo warto przyciąć PNG do treści)
5. **Tło-perła jest martwe/jednolite** — Kuba nie widzi „perły" (sampling zlewa
   stronę z wideo w jeden płaski kolor). Kierunek: perła MUSI się delikatnie mienić,
   jak masa perłowa przy ruchu głowy. **Do pogadania — podrzucić pomysły na
   aktywność tła** (temat otwarty, nic nie zaimplementowane).

## Zrobione po decyzjach (15:46–15:50)
- ✅ wypalone `video/intro_F2EFEA.mp4` (476 KB, bake_variant z cache `_work/intro/`)
- ✅ `scena-final.html` ustawiona na finalne wartości: wideo F2EFEA (intro+loop),
  CTA fiolet `#6B2FD9`/biały, `--bg #F2EFEA`, panel roboczy z zaznaczonymi wyborami

## Runda 2 feedbacku (~16:20) — layout hero + glow
- **„Perła z testowych ekranów"** = w playgroundzie glow renderował się OD RAZU (idle,
  środek sceny) + była winieta pod wideo; w scenie glow czekał na mousemove → płasko.
  NAPRAWIONE: glow spoczynkowy nad Sansitem + winieta `#vig` (vg=10) przeniesiona.
- **Sansit wbiegał od 1/4 ekranu** (wideo wyśrodkowane max-w 880px). Kubie zależy na
  wrażeniu, że wbiega ZZA EKRANU + wykorzystać pełną szerokość intro (nie tracić drogi).
  ZROBIONE: wideo przypięte do lewej krawędzi ekranu, flex 66% szerokości → postać
  kończy w ~1/3; prawa 1/3 = h1+copy+CTA (przeniesione z dołu, wyrównane do lewej);
  mobile (<820px): kolumna, tekst pod wideo. Krawędzie wideo niewidoczne dzięki samplingowi.
- **Kierunek tła wybrany przez Kubę: DUŻY glow** (jak przy śledzeniu kursora) →
  preset „Mocna scena" z playgroundu: promień 560px, intensywność 26%, plus **druga
  warstwa glow NAD wideo** (`#glowOver`, mix-blend-mode:multiply, opacity .5) — wcześniej
  glow chował się pod wideo. Pomysł „mienienia się" perły (pasma interferencyjne + sheen)
  dalej otwarty jako dodatek.

## Runda 3 feedbacku (~16:30) — ALPHA + żywa perła
- Kuba: „to już jest dobra droga… wbiegnięcie robi mega wrażenie". Teksty hero —
  wciąż do rozmowy (nieprzekonany).
- **Problem: wideo ma wpalone tło** (widać różnicę przy glow i w ogóle) → Kuba poprosił
  o wycięcie postaci NA ALFĘ, żeby próby tła nic nie kosztowały. ZROBIONE:
  - `_tools/bake_alpha.py` — składa postać+cień na przezroczystości z cache
    (`_work/<clip>/graded+mask`) → **VP9 alpha .webm** (yuva420p; UWAGA:
    `-auto-alt-ref 0` jest wymagane dla alfy w libvpx)
  - `video/intro_alpha.webm` (1.25 MB) + `video/loop_alpha.webm` (1.14 MB)
  - scena: feature-detect VP9 → gra webm-alpha; fallback mp4 (Safari / brak pliku,
    handler na `error`); przy alfie sampling tła i winieta WYŁĄCZONE
  - ⚠️ Safari/iOS nie gra VP9-alpha → docelowo HEVC-alpha (do wypalenia na Macu)
  - ⚠️ lekkość: webm-alpha ~2.4 MB łącznie vs mp4 ~0.8 MB — przed premierą sprawdzić
    czy crf 32→36 / downscale nie wystarczy
- **Kierunek tła od Kuby: cała canva jak glow** — perła zielono-magenta-fiolet,
  leciutka, W CIĄGŁYM delikatnym ruchu („refleks"). ZROBIONE (wersja 1):
  `#ambient` — 3 ogromne rozmyte plamy (zieleń .14 / magenta .10 / fiolet .11),
  blur 90px, dryf 47/61/53 s ease-in-out alternate. Do strojenia z Kubą.

## Runda 4 feedbacku (~16:40) — szlify sceny
Kuba: kierunek dobry; technika najpierw, sekcja emocji może zmienić miejsce (później);
teksty hero wciąż do rozmowy.
- **Kreska pod Sansitem ucinająca glow** → warstwy glow rozszerzone 180px poniżej hero
- **Glow kursora zmniejszony**: 560px/26% → 480px/20%
- **Za mało perły i dynamiki w tle** → ambient podbity (alfy .20/.15/.16, plamy 65vmax,
  pętle 34/44/39 s) + NOWY jasny „refleks perłowy" (.d, 80vmax, biel .55, wędruje 27 s)
- **Skok koloru intro→loop ZMIERZONY i NAPRAWIONY**: loop był ciemniejszy ~3.5%
  (fg intro 169.6 vs loop 163.7); w `bake_alpha.py` gain per kanał
  (R×1.030, G×1.031, B×1.053) wyrównuje loop do intro — wpisany na stałe ze zmierzonych wartości
- **„Paintowy" obrys (gł. przy głowie)** → maska: MinFilter(3) erozja 1px + GaussianBlur 1.6
  piórko; NOWA poświata `#halo` za Sansitem (pomysł Kuby — jasny miękki radial, oddech 7 s)
  dodatkowo wtapia obrys w tło
- Bonus: piórko poprawiło kompresję — webm-y 2.4 MB → ~1.5 MB łącznie (868+646 KB)

## Runda 5 (~17:00) — playground perły + naprawa szwu loopa
- Kuba klepnął pomysł playgroundu → **`_tools/perla-playground.html`** (wzorzec jak
  hero-blend: panel suwaków / podgląd na żywo z prawdziwym loop_alpha / „Kopiuj ustawienia").
  DEFAULTS = wartości z obecnej scena-final.html; prompt wypisuje TYLKO zmiany.
  Suwaki: tło bazowe, plamy (kolory/intensywność/rozmiar/blur/tempo), refleks perłowy,
  halo (siła/rozmiar/pozycja/oddech), glow kursora. Presety: Obecna scena / Perła mocniej /
  Ledwo-ledwo / Zorza.
- **Kuba zauważył „rozciąganie" Sansita na zawinięciu loopa** → ZMIERZONE po maskach:
  źródłowy loop ma na szwie klatki odstające ~7px w dół (0001: bottom=658, 0142-0145:
  655-658; mediana reszty: 651). **Fix: trim do klatek 0002-0141** (140 kl.; szew 141→2
  gładki, 0 px różnicy). `bake_alpha.py` przyjmuje teraz opcjonalny trim
  (`bake_alpha.py loop out.webm 2 141`), czyści i renumeruje klatki out.
- Bonus: intro kończy się DOKŁADNIE w pozie klatki 0002 (55/651) — trim naprawił też
  7px skok na sklejce intro→loop.
- `loop_alpha.webm` po trimie: 604 KB.
- ⚠️ mp4 fallbacki (F2EFEA itd.) nadal mają stary szew — przepalić przy finalizacji
  (`bake_variant.py` nie ma trimu; dodać analogicznie jak w bake_alpha).

## Runda 6 (~17:30) — nastawy finalne, index.html, GitHub Pages
- Playground: przebudowa na RÓJ (feedback Kuby: „dzieje się tylko w prawym dolnym rogu",
  chciał mniejsze×więcej×dynamiczniej) — plamy rozrzucone złotym kątem po całej canvie,
  organiczny dryf sin/cos przez rAF. Fix „skakania": zegar PRZYROSTOWY (suwak tempa nie
  teleportuje plam) + refleks na transform (GPU) zamiast left/top.
- **NASTAWY FINALNE Kuby (playground → schowek):** 10 plam, intensywność 150%
  (alfy .30/.225/.24), rozmiar 49vmax, tempo 225%; reszta default (blur 90, refleks
  .55/80vmax/27s po przekątnej, halo .85/44%/12,8%/7s, glow 480px/20%).
- **`index.html` w korzeniu = PRODUKCJA** (czysty, bez paneli roboczych): pełna scena
  hero + rój + emocje; ścieżki względne od korzenia. `_tools/scena-final.html` = legacy
  warsztat (ma stary ambient CSS); strojenie tła od teraz w `perla-playground.html`.
- Decyzje: repo PUBLICZNE (premiera za chwilę, brak ryzyka wg Kuby); `_DOCS/` dodane
  do .gitignore (PRD/brief zostają lokalnie). Hosting docelowy do wyboru:
  GitHub Pages z domeną (rekomendacja) vs LiteSpeed jak qubatura.
- Klepnięte przez Kubę: po opanowaniu tła → GitHub do pokazywania designu, potem reszta.

## Następne kroki
1. Kuba odtwarza swoje sugestie/obawy z urwanej sesji → dopisać do handoffa
2. Kuba ogląda scenę po zmianach (fiolet + porcelana) — potwierdzenie wizualne
3. Przeniesienie sceny z `_tools/` do właściwego `index.html` (produkcyjnie: bez panelu
   roboczego; pliki `*_TEST.mp4` i nieużywane warianty do skasowania dopiero wtedy)
4. Reszta strony wg `_DOCS/SANSIT_WEBSITE_PRD_v2.md` (+ `SANSIT_ABOUT_brief.md`)
5. PRD dokłada Kuba dziś (2026-07-16) — sprawdzić czy v2 w `_DOCS` jest już aktualne
