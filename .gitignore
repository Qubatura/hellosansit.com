# System
desktop.ini
Thumbs.db
.DS_Store

# Narzędzia lokalne (Real-ESRGAN itp.) — nie wersjonujemy
_tools/

# Pliki robocze / pośrednie (klatki wideo itp.)
_work/

# Zaplecze — repo jest PUBLICZNE (GitHub Pages), PRD/briefy zostają lokalnie
_DOCS/

# Surowe assety książki z Canvy (ciężkie, ~9 MB) — do repo idą tylko lekkie *_web/spread_*.webp
book/*.png

# Notatki robocze / kontekst projektu — NIE publikujemy pod domeną (repo = web-root na IQ)
SESSION-*.md
CLAUDE.md
*-stan.md
